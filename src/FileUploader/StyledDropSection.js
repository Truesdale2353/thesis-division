import styled from "styled-components";

const StyledDropSection = styled.div`
  height:100%;
  background: #f8f9fd 0% 0% no-repeat padding-box;
  border-radius: 25px;
  opacity: 1;
  margin: 20px;
  text-align:center;
  border: 2px dashed #36489F;
  justify-content: center;
`;

export { StyledDropSection };

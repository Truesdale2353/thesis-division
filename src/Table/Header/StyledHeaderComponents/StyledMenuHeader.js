import { Typography } from "@mui/material";
import styled from "styled-components";

const StyledMenuHeader = styled(Typography)`
  && {
    font-family: "Futura";
    text-align: center;
    color: #23252f;
    margin-top: 20px;
    font-weight: 600;
    font-size: 1.5 rem;
    line-height: 1.5;
    letter-spacing: 0em;
  }
`;

export { StyledMenuHeader };

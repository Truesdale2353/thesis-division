import { Typography } from "@mui/material";
import styled from "styled-components";

const StyledSortingLabel = styled(Typography)`
  && {
    font-family: "Poppins";
    text-align: center;
    color: #23252f;
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.5;
    letter-spacing: 0em;
  }
`;

export { StyledSortingLabel };
